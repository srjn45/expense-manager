import { CategoriesScreen } from '@/features/categories'

/** `/categories` route (§8 Phase 3). Reached from the home screen. */
export default function CategoriesRoute() {
  return <CategoriesScreen />
}
