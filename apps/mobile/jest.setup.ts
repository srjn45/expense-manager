// Jest setup. @testing-library/react-native (v13+) registers its matchers
// automatically, so nothing is required here yet. Phase 1+ can add global mocks
// (e.g. an in-memory better-sqlite3 Drizzle instance) as needed.
export {}
